{
  "additionalProperties": false,
  "properties": {
    "action": {
      "enum": [
        "pause",
        "resume",
        "reschedule"
      ],
      "title": "Action",
      "type": "string"
    },
    "due_at": {
      "anyOf": [
        {
          "format": "date-time",
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "title": "Due At"
    },
    "expected_revision": {
      "minimum": 1,
      "title": "Expected Revision",
      "type": "integer"
    }
  },
  "required": [
    "expected_revision",
    "action"
  ],
  "title": "ReviewUpdateBody",
  "type": "object"
}
