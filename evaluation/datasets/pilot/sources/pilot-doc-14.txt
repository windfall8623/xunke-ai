# 银铃门禁演示权限表

本资料为模型起草的虚构测试规程，不构成现实操作建议。

主体：每个合成主体使用唯一主体编号。

角色：角色分为访客、值班员和管理员。

访客：访客只能读取公开展示区信息。

值班员：值班员可读取当班房间的状态。

管理员：管理员可配置演示授权但不得查看其他主体的私有笔记。

失效：过期授权在请求执行时重新检查。

撤回：撤回授权后缓存结果不得继续返回。

审计：拒绝日志记录请求编号，不记录私有正文。

隔离：权限演练只能操作合成主体和测试房间。

复用：同名主体不能因此合并身份。
